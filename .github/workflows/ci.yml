on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: CI Pipeline
    steps:
      - uses: actions/checkout@v3
      - name: Run Tests
        run: echo '✅ CI triggered via Prime reflex 1.1.3'
