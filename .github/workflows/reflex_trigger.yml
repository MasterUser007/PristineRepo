name: Reflex Trigger

on:
  workflow_dispatch:

jobs:
  reflex-confirm:
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Dispatch reached
        run: echo "Reflex trigger dispatched successfully!"
