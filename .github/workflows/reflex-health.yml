name: Reflex Health Monitor
on:
  schedule:
    - cron: "0 12 * * 1"  # Every Monday at noon UTC
jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run Reflex Health Check
      shell: pwsh
      run: pwsh ./scripts/ReflexHealthCheck.ps1
