# 🔒 Deprecated by ReflexOps
name: Tombstoned: prime.sdlc.flowmap.yml
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  tombstone:
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated Workflow Exit
        run: echo '🛑 prime.sdlc.flowmap.yml is deprecated and exits silently.'
