name: Prime Reflex Watchdog
on:
  schedule:
    - cron: '0 3 * * *'  # Daily at 3 AM UTC
jobs:
  watchdog:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run Reflex Health Check
      shell: pwsh
      run: pwsh ./scripts/ReflexHealthCheck.ps1

# trigger metadata reload
