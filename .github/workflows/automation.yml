# 🔒 Deprecated by ReflexOps
name: Tombstoned: automation.yml
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  tombstone:
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated Workflow Exit
        run: echo '🛑 automation.yml is deprecated and exits silently.'
